---
name: purchase-order-preparation
description: |
  주문할 품목을 마스터에서 식별하고(없으면 초안 등록), 공급처별 단가·리드타임·최소주문수량(MOQ)·거래 조건을
  나란히 정리한 뒤, 공급처별로 묶어 주문수량을 MOQ·포장 단위에 맞춰 올리고 납기 희망일을 산정해
  주문서 초안을 미확정 상태로 만든다. 주어진 정책 규칙에 걸리는지도 판정한다. 확정·전송은 하지 않는다.
  다음과 같은 요청에 사용할 것 — "발주서 초안 만들어줘", "RFQ 작성", "구매요청서 뽑아줘", "견적서에서 품목 정보 뽑아줘",
  "이 품목 코드 있는지 확인해줘", "신규 품목 등록해줘", "공급처별 단가 비교", "어디가 제일 싸/빨라?", "리드타임 확인",
  "MOQ 얼마야", "발주수량 계산해줘", "MOQ 맞춰서 수량 올려줘", "공급처별로 묶어줘", "납기 희망일 언제로 넣지",
  "이 발주 승인 받아야 하나?", "결재 대상인지 확인", "단독 공급처인 품목 찾아줘", "외주 발주서 초안", "창고 이동 요청서".
  자재·외주 가공·서비스 계약 등 조달 대상을 가리지 않고, 구매 발주·RFQ·이동 요청·생산 요청 등 주문 문서면 종류를 가리지 않는다.
  다음에는 사용하지 않는다 — 소요량·순소요량 계산은 net-requirement-planning.
  주문서 확정·전송·취소, 공급처 선택 결정, 마스터 확정, 승인 여부 결정, 입고 검수는 이 스킬의 범위가 아니다.
  승인 규칙을 받지 못하면 승인 판정을 하지 않는다.
---

# Purchase Order Preparation

**무엇을 어디서 얼마에 언제 받을 수 있고, 그래서 주문서를 어떻게 쓰나.**
순소요량을 받아 실제 주문 문서 직전까지 데려간다.

## 핵심 원칙

**확정하지 않는다.** 주문서는 반드시 미확정 상태로 남긴다.
확정을 실행하는 것은 사람의 몫이고, 이 경계가 자동화의 안전선이다.

**공급처를 대신 고르지 않는다.** 출력은 선택지이지 결정이 아니다.
단가만 보면 리드타임을 놓치고, 리드타임만 보면 MOQ를 놓친다.

**모르는 값은 비워 둔다.** 확신도가 낮으면 「확인 필요」로 표시한다.
**비어 있는 값은 사람이 채우지만, 틀린 값은 아무도 고치지 않는다.**

**빈 값으로 문서를 만들지 않는다.** 공급처가 없거나 조건이 비면 보류한다.
`scripts/run.py`가 보류 품목을 주문서 단계로 넘기지 않고, 넘어갔으면 오류를 내고 멈춘다.

**승인 여부를 판단하지 않는다.** "정책 조건에 해당한다"까지가 이 스킬의 몫이다.
**승인 규칙을 받지 못하면 판정 자체를 하지 않는다.** 임의 기준을 만들면 통제가 무력화된다.

**조건을 맞추려고 수량을 늘리지 않는다.** 무료배송 기준에 근소 미달이면 그 사실만 메모에 남긴다.

## 경계

| 한다 | 하지 않는다 |
|---|---|
| 품목 마스터 조회, 유사 품목 탐색 | 소요량 계산 → `net-requirement-planning` (입력으로 받는다) |
| 미등록 품목 초안 등록 | **마스터 확정** — 사람이 한다 |
| 공급 조건 정규화·비교표 | **공급처 선택 결정** — 사람이 한다 |
| 주문수량 올림 (MOQ·포장 단위) | 재고 데이터 수정 |
| 납기 희망일 산정, 공급처별 묶음 | **주문서 확정·전송·취소** — 사람이 한다 |
| 정책 규칙 해당 여부 판정 | **승인 여부 결정**, 입고 검수 |

## 워크플로

### 1. 입력 수집

| 항목 | 필수 | 내용 |
|---|---|---|
| `order_candidates` | ● | 앞 스킬의 주문 대상 — 품목 · 순소요량 · 긴급도 · 신규 여부 |
| `supply_terms` | ● | 공급처별 단가 · 리드타임 · MOQ · 포장 단위 · 거래 조건 |
| `target_items` | ○ | 품목 식별 결과. 생략하면 `order_candidates`로 구성 |
| `vendor_selection` | ○ | 사람이 고른 공급처 |
| `on_hand` · `target_level` · `need_by` | ○ | 목표재고 초과 · 납기 부족 판정용 |

**순소요량을 다시 계산하지 않는다.** 받은 값을 그대로 쓴다.

### 2. 실행

```bash
python scripts/run.py --input data.json --params params.json --output result.json
```

두 단계를 순서대로 돌리고 결과를 합쳐 낸다. 단계별로 따로 돌려도 된다.

```bash
python scripts/phase1-sourcing/normalize_terms.py --input d.json --params p.json
python scripts/phase2-drafting/draft_orders.py    --input d.json --params p.json
```

파라미터는 호출하는 쪽이 준다. 목록과 기본값은
`reference/parameters-phase1.md` · `parameters-phase2.md`.
수량·납기 규칙은 `reference/quantity-rules.md`, 정책 판정은 `reference/approval-rules.md`.

> `allow_confirm`은 자리만 있고 잠겨 있다. `true`를 넣으면 스크립트가 오류를 낸다.
> "나중에 자동 확정을 켜면 된다"는 여지를 남기지 않기 위한 장치다.

### 3. 보류 처리

**보류는 실패가 아니다.** `blocked_items`·`held_items`에 사유와 필요한 조치를 담아 낸다.
받아 줄 곳이 없으면 사람은 "왜 문서가 안 나오지"에서 멈춘다. **반드시 사람에게 노출한다.**

| 분류 | 뜻 | 필요한 조치 |
|---|---|---|
| `no_supplier` | 공급 조건이 **0건** — 공급처 자체가 없다 | 소싱 요청 |
| `terms_incomplete` | 공급처는 있으나 필수 조건 일부가 빔 | 빈 값 보완 |
| `draft_only` | 마스터 초안만 있고 확정되지 않음 | 마스터 확정 |

**`no_supplier`와 `terms_incomplete`를 섞지 말 것.** 필요한 조치가 다르다.

### 4. 리포트 작성

`templates/comparison.md`(품목 확인·공급 조건)와 `templates/order-draft.md`(주문서 초안)의
섹션 구조를 이어 붙여 하나의 리포트로 낸다. 정책 판정과 근거를 맨 뒤에 둔다.

## 수량 결정 규칙

```
기본 수량 = 순소요량
  → MOQ 미달이면      MOQ로 올림
  → 포장 단위가 있으면 그 배수로 올림
```

올림이 일어난 품목은 **원래 소요량과 최종 수량을 함께 적는다.**
왜 15개가 필요한데 20개를 사는지 사람이 알 수 있어야 한다.

**목표재고를 넘어도 수량을 줄이지 않는다.** 초과 사실만 메모에 남긴다.
줄이면 MOQ·포장 단위 위반으로 주문이 거절된다.

## 출력

| 필드 | 내용 |
|---|---|
| `sourcing.identified_items` | 품목 · 식별 상태 · 유사 품목 후보 · 확인 필요 항목 |
| `sourcing.draft_items_created` | 이번에 초안 등록한 품목 |
| `sourcing.supply_options` | 품목 × 공급처별 조건 비교 — `single_source` 등 플래그 포함 |
| `sourcing.long_lead_items` | 장납기 기준 초과 (기준을 받았을 때만) |
| `sourcing.blocked_items` | 공급처 없음 · 조건 미비 — **사람이 보완할 것** |
| `drafting.order_drafts` | 공급처별 주문서 초안 — 품목 · 수량 · 올림 사유 · 단가 · 납기 |
| `drafting.draft_summary` | 문서별 합계 |
| `drafting.special_requirements` | 품목 성격상 필요한 첨부·보관 조건 |
| `drafting.policy_assessment` | 문서별 판정 · **걸린 규칙 · 근거** |
| `drafting.held_items` | 문서를 만들지 못한 품목 |
| **`requires_approval`** | boolean — 분기용 |

**필드 이름은 이 스킬이 정의하는 논리 이름이다.** 실제 폼·변수와의 연결은 호출하는 쪽이 매핑한다.

## 품질 체크리스트

- [ ] 유사 품목 후보를 자동 확정하지 않고 후보로 제시했는가
- [ ] 확신도 낮은 값을 추측해 채우지 않았는가
- [ ] 복수 공급처를 나란히 제시했는가 (임의 선택 금지)
- [ ] 리드타임이 범위면 긴 쪽을 쓰고 원문을 남겼는가
- [ ] **보류 품목으로 문서를 만들지 않았는가**
- [ ] 올림이 일어난 품목에 원래 소요량을 함께 적었는가
- [ ] **모든 문서가 미확정 상태인가**
- [ ] 판정에 걸린 규칙과 근거를 모두 적었는가
- [ ] 승인 규칙을 받지 못했으면 판정을 하지 않고 그 사실을 보고했는가

## 파일 구성

| 경로 | 언제 읽나 |
|---|---|
| `scripts/run.py` | **항상** — 두 단계를 순서대로 실행 |
| `reference/parameters-phase1.md` · `-phase2.md` | 설정값을 정하거나 확인할 때 |
| `reference/item-identification.md` | 품목이 마스터에 있는지 애매할 때 |
| `reference/extraction-fields.md` | 견적서·카탈로그에서 값을 뽑을 때 |
| `reference/normalization-rules.md` | 단가·리드타임 표기가 제각각일 때 |
| `reference/quantity-rules.md` | 수량 올림·납기 산정을 검증할 때 |
| `reference/approval-rules.md` | 정책 판정 규칙을 확인할 때 |
| `reference/special-requirements.md` | 첨부·보관 조건이 필요한 품목일 때 |
| `templates/comparison.md` · `order-draft.md` | 리포트를 쓸 때 |
| `scripts/phase*/test_*.py` | 스크립트를 고쳤을 때 |

## 프로세스에 붙일 때

1. **입력 연결** — 품목 마스터와 공급 조건을 어디서 읽고, 초안을 어디에 만들지 지정한다.
2. **파라미터 주입** — 특히 `approval_rules`. **주입하지 않으면 정책 판정을 하지 않는다.**
3. **출력 매핑** — 출력 필드를 폼 필드·프로세스 변수에 연결한다. 분기에는 `requires_approval`.
4. **받아 줄 곳 지정** — `blocked_items` · `held_items` · `similar_candidates`를
   사람이 어디서 보완할지 정한다. 이것이 없으면 보류가 막다른 길이 된다.

이 네 가지를 프로세스 쪽 문서에 표로 정리해 두면, 프로세스가 바뀌어도
이 스킬은 그대로 두고 매핑표만 고치면 된다.
